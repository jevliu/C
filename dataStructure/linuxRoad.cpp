/*
最简linux路径问题
2023/3/5
*/
#include <iostream>
#include <cstring>
using namespace std;

const int MAX_NUM = 10000;
char c[MAX_NUM]; // 定义一个具有最大长度的字符串型数组来做栈
int top = -1;    // 定义一个指向栈顶的指针,-1表示当前栈为空

int main()
{
    cin >> c;
    int n = strlen(c);
    for (int i = 0; i < n; i++)
    {
        if ()
    }
}